# Rightly - offline one-click deploy dependencies.
# Installed automatically by CaiDat-Rightly.bat (or: pip install -r requirements-deploy.txt)
# Everything here runs locally; no cloud API is required.

# Web server (HTML UI at http://localhost:8010)
fastapi>=0.111
uvicorn>=0.30
python-dotenv>=1.0
python-multipart>=0.0.9

# Local LLM (Ollama, OpenAI-compatible endpoint)
openai>=1.40

# Local Whisper ASR (Vietnamese speech -> text, fully offline)
faster-whisper>=1.0
av>=12.0
tqdm>=4.66
huggingface_hub>=0.23

# Retrieval
numpy>=1.26
openvino>=2025.0
torch>=2.2
transformers>=4.41
sentence-transformers>=3.0

# TTS fallbacks (browser speech is used by default; these are backups)
requests>=2.31
edge-tts>=6.1.0
gTTS>=2.5.0
piper-tts>=1.2.0
cryptography>=42.0

# Native desktop wrapper (the installer builds Rightly.exe after preflight).
pyinstaller>=6.0
pywebview>=5.0
