# Copy these settings into Streamlit Cloud > App settings > Secrets.
# This configuration uses the real GPT-5.6 Luna backend through Pateway.
# Put the real key only in Streamlit Cloud Secrets, never in GitHub.

APP_MODE = "cloud"
LLM_BACKEND = "pateway"
LLM_FALLBACK_BACKEND = ""
PATEWAY_API_KEY = "paste-your-pateway-api-key-here"
PATEWAY_BASE_URL = "https://api.pateway.ai/v1"
PATEWAY_MODEL = "gpt-5.6-luna"

# Keep TTS disabled unless you also configure a TTS provider.
TTS_BACKEND = "mock"

# Safe demo defaults.
RETRIEVAL_BACKEND = "bm25"
SAVE_TRANSCRIPTS = "false"
DELETE_RAW_AUDIO_AFTER_SESSION = "true"
PII_SCRUB_OUTBOUND = "true"
